<div class="card">
  <div class="card-header">
    <h2>{{post.title}}</h2>
    <span>Posted by {{post.author}} on {{formatDate post.createdAt}}</span>
  </div>
  <div class="card-body">
    <p>{{post.content}}</p>
  </div>
</div>